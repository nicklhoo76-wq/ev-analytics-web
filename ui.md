你现在需要对一个已有的“充电管理平台大屏”前端进行 UI 视觉重构。

【一、项目技术栈】

项目技术栈：
- 前端：Vue
- 图表：ECharts
- 后端：Flask
- 现有页面已经具备完整的业务功能、数据展示和交互逻辑。

本次任务只进行【前端 UI / UX 视觉层改造】，不要重新设计业务系统。

绝对不要修改：
1. Flask 后端接口
2. API URL
3. API 请求参数
4. API 返回数据结构
5. 数据处理逻辑
6. 业务逻辑
7. 页面已有功能
8. 页面整体布局和模块位置
9. 已有路由
10. 已有站点、订单、负荷、充电量、收益、预测等数据逻辑

如果现有代码中的数据字段、接口或组件已经可以正常工作，应直接复用。

--------------------------------------------------
【二、整体设计目标】
--------------------------------------------------

将现有深色“传统科技大屏”视觉风格升级为：

“浅色未来能源 · 城市充电网络数字孪生”

设计关键词：

- 浅色
- 明亮
- 现代
- 简洁
- 新能源
- 智慧城市
- 轻量数字孪生
- 数据可视化
- 高级感
- 未来科技感

注意：

不是赛博朋克风。
不是游戏 HUD。
不是传统深色数据大屏。
不是普通企业后台。
不是复杂 3D 数字孪生。
不是复杂地图系统。

整体应该类似：
“高端新能源运营中心 + 智慧城市数据平台”。

未来科技感主要通过：
- 配色
- 细线
- 数据层级
- 微弱渐变
- 节点状态
- 少量动态效果
- ECharts 图表细节

来体现。

禁止通过大量霓虹发光、粗边框、粒子、扫描线来制造科技感。

--------------------------------------------------
【三、页面整体布局】
--------------------------------------------------

必须保留现有页面布局。

现有页面为大屏 Dashboard，布局结构已经确定，不允许重新排列。

第一张页面继续保持：

顶部：
1. Logo / “充能脉络”
2. “全域态势”
3. “充电时空”
4. 城市位置
5. 暂停 / 全屏等操作
6. 模型评估、数据时间点、站点选择

第二层：
站点筛选：
- 全网站
- 海淀区 · 001站
- 朝阳区 · 002站
- 丰台区 · 003站
- 石景山区 · 004站
- 房山区 · 005站
- 大兴区 · 006站
- 通州区 · 007站
- 顺义区 · 008站

第三层：
KPI 数据区域：
- 覆盖站点
- 最近小时负荷
- 今日充电量
- 当前空闲
- 今日净营收

主体区域继续保持现有三列结构。

左列：
- 订单概览
- 站点负荷排行
- 小时充电量

中列：
- 北京 / 城市站点空间分布
- 站点 × 小时负荷热力图
- 站点可用性与状态

右列：
- 负荷趋势与预测
- 未来空闲充电位
- 负荷预警

第二张“充电时空”页面同样保持当前布局和模块，不允许改变功能和模块位置。

--------------------------------------------------
【四、整体颜色】
--------------------------------------------------

从现有深蓝色主题改成明亮浅色主题。

页面背景：

#F3F7FA

主卡片：

#FFFFFF

次级背景：

#F7FAFC

主文字：

#18324D

次级文字：

#6F8296

辅助文字：

#9AAABD

主色 / 新能源青：

#19C7C9

辅助科技蓝：

#4D8EF7

蓝紫：

#7167E8

正常状态：

#25C9A5

预警：

#FFAA45

异常：

#FF647C

边框：

#DCE7EF

分割线：

#E8EFF4

所有颜色必须低饱和、干净、明亮。

禁止：
- 大面积纯黑
- 大面积深蓝
- 高饱和荧光绿
- 大面积紫色
- 霓虹渐变
- 彩虹渐变

--------------------------------------------------
【五、卡片设计】
--------------------------------------------------

现有卡片统一重构。

卡片：

background: #FFFFFF

border:
1px solid #E3ECF2

border-radius:
10px ~ 14px

不要使用明显的蓝色粗边框。

不要使用四周明显发光。

阴影非常轻：

0 4px 16px rgba(30, 70, 100, 0.06)

卡片之间保持明确的空间关系。

每个卡片标题左侧保留一个非常细的新能源青色竖线：

宽度：2~3px
高度：14~18px

例如：

▎订单概览

而不是：

[发光蓝色边框] 订单概览

卡片标题：
- 深蓝色
- 中等字重
- 14~16px

辅助信息：
- 11~12px
- 灰蓝色

数据数字：
- 26~32px
- 深蓝
- 适当使用主色强调

--------------------------------------------------
【六、顶部 Logo 和导航】
--------------------------------------------------

保留“充能脉络”。

Logo 使用：
- 深蓝文字
- 青色闪电 / 能源图标
- 不要使用强烈霓虹发光

顶部导航：

“全域态势”
GLOBAL OVERVIEW

“充电时空”
CHARGING SPACE

中文为主要文字，英文作为非常小的辅助文字。

当前选中的 Tab：
- 使用浅青色背景
- 底部增加一条 2~3px 青色指示线
- 可以有非常轻微的渐变
- 不要使用大面积蓝色填充

--------------------------------------------------
【七、站点筛选】
--------------------------------------------------

站点筛选不要再使用传统深色矩形按钮。

改成现代 Capsule / Chip 风格。

普通状态：

白色背景
浅灰边框
深蓝文字
圆角 6~8px

选中状态：

浅青色背景
青色边框
青色文字
非常轻微的青色阴影

例如：

全网站

海淀区 · 001站

朝阳区 · 002站

大兴区 · 006站

当前选择“大兴区 · 006站”时明显突出。

但不要使用强烈发光。

--------------------------------------------------
【八、顶部 KPI】
--------------------------------------------------

五个 KPI 卡片保持原来的位置和数量。

每个 KPI 使用：

标题
↓
超大数字
↓
单位
↓
辅助状态信息

例如：

覆盖站点

1 座

● 2 个充电位

数字应该成为视觉重点。

不同指标可以使用不同的小型图形装饰：

覆盖站点：
小型站点图标

小时负荷：
小型趋势线

今日充电量：
小型柱状趋势

当前空闲：
小型环形图

今日净营收：
小型趋势线

这些装饰只能作为辅助视觉元素，不能抢占主要数据。

--------------------------------------------------
【九、核心区域：城市充电网络空间分布】
--------------------------------------------------

这是本次改造的重点。

**中央区域不是地图，而是城市充电网络空间拓扑；站点坐标只是相对坐标，不是经纬度。**

注意：

项目无法使用真实北京地图。
不要尝试加载真实地图。
不要引入高德地图、百度地图、Mapbox、Google Maps、Cesium、Three.js 等地图或 3D 框架。

这里不是地理地图。

这里是：

“城市充电网络空间拓扑 / 轻量数字孪生视图”。

继续使用现有站点的相对位置。

例如：

海淀区
石景山区
丰台区
朝阳区
大兴区
通州区
顺义区

按照当前页面中已有的相对位置进行展示。

站点坐标只是画布中的相对坐标，例如：

x = 0~100
y = 0~100

不是经纬度。

--------------------------------------------------
【十、空间拓扑视觉】
--------------------------------------------------

背景：

白色 / 极浅蓝灰。

可以增加非常淡的：
- 网格
- 同心圆
- 几何城市轮廓
- 极淡的连接结构

透明度必须非常低。

背景不能抢夺站点的视觉注意力。

不要画假的北京地图轮廓。

不要画真实道路。

不要画复杂行政区。

不要做 3D 建筑。

不要做复杂城市模型。

--------------------------------------------------
【十一、充电站节点】
--------------------------------------------------

每个站点使用：

中心圆点 + 柔和外圈

普通站点：

青色中心点

充电中：

蓝色中心点

预警：

橙色中心点

异常：

红色中心点

节点外围可以有非常淡的半透明光晕。

光晕必须非常克制。

不要使用赛博朋克式强烈发光。

站点旁边显示：

大兴区
006站 · 2空闲

其中：
“区域名称”为主要文字。
“站点编号 / 空闲数量”为小号辅助文字。

--------------------------------------------------
【十二、当前选中站点】
--------------------------------------------------

当前选中：

“大兴区 · 006站”

必须明显于普通站点，但不要夸张。

使用：

中心圆点
+
双层圆环
+
轻微光晕

并显示简洁浮动信息：

大兴区 · 006站
在线 2/2
当前负荷 0.0 kW

信息卡采用白色半透明 / 白色卡片。

圆角 8px。

轻阴影。

不要使用大面积深色 Tooltip。

--------------------------------------------------
【十三、站点之间的网络关系】
--------------------------------------------------

可以增加非常少量的站点连接线。

注意：

不是所有站点都必须连接。

只连接与当前选中站点相关的少量节点。

连接线：

- 极细
- 浅青色
- 低透明度
- 虚线或细实线

可以增加一个很小的动态光点在线路上缓慢移动。

这个动态光点表示：

“能源网络实时运行”。

动画必须缓慢、低调。

不要出现大量移动粒子。

不要出现满屏流光。

--------------------------------------------------
【十四、ECharts 图表统一设计】
--------------------------------------------------

所有 ECharts 必须统一视觉语言。

统一要求：

- 去掉粗坐标轴
- 坐标轴使用浅灰色
- Grid 使用极浅灰蓝
- 不使用明显黑色坐标轴
- 不使用默认 ECharts 颜色
- 所有图表颜色使用项目统一色板
- Tooltip 使用白色卡片
- Tooltip 有轻微阴影
- 圆角 8px
- 字体使用深蓝灰
- 图表整体留白合理

--------------------------------------------------
【十五、负荷趋势与预测】
--------------------------------------------------

历史负荷：
使用青色平滑折线。

预测负荷：
使用蓝紫色虚线。

预测区域：
增加非常淡的预测置信区间。

例如：

历史：
━━━━━━━

预测：
- - - - - -

预测范围：
浅蓝 / 浅紫半透明区域

预测阈值：
使用橙色细虚线。

不要使用粗线。

不要使用高饱和色块。

让图表表现出：
“实时监测 + AI预测”。

--------------------------------------------------
【十六、订单概览】
--------------------------------------------------

保留环形图。

中心显示：

1
笔订单总数

环形图使用：
青色主色。

其他状态使用低饱和辅助色。

不要使用传统 3D 饼图。

不要使用渐变过度。

--------------------------------------------------
【十七、站点负荷排行】
--------------------------------------------------

保留排行榜。

使用：

排名
站点名称
横向进度条
负荷数值

进度条使用青色。

第一名可以稍微增强视觉。

不要使用复杂动画。

--------------------------------------------------
【十八、小时充电量】
--------------------------------------------------

可以继续使用柱状图。

柱子使用：
青色 / 蓝色系。

柱子：
圆角顶部。

不使用立体柱。

不使用渐变过度。

--------------------------------------------------
【十九、站点 × 小时负荷热力图】
--------------------------------------------------

保留 ECharts Heatmap。

低负荷：

极浅蓝灰

中负荷：

浅青

高负荷：

青色 → 蓝色 → 少量紫色

整体保持低饱和。

不要使用彩虹色热力图。

--------------------------------------------------
【二十、站点可用性与状态】
--------------------------------------------------

继续保持现在的卡片布局。

使用：
横向进度条 + 数字。

例如：

大兴区       2 / 2

████████████

正常状态使用青色。

如果部分占用：
青色 + 浅蓝 / 紫色。

异常：
橙色 / 红色。

整体简洁。

--------------------------------------------------
【二十一、未来空闲充电位】
--------------------------------------------------

继续使用柱状图。

重点表现：

未来不同时间的空闲充电位数量。

当前时间可以使用青色。

未来时间使用浅蓝 / 蓝紫。

柱子使用圆角。

保持轻量。

--------------------------------------------------
【二十二、负荷预警】
--------------------------------------------------

如果没有预警：

不要显示复杂的空状态插画。

使用非常简洁的：

✓ 当前暂无超限

或者：

当前可用预测波动范围内暂无超限

可以配一个极简线性图标。

如果存在预警：
使用橙色作为主要提示色。

严重异常才使用红色。

--------------------------------------------------
【二十三、第二张“充电时空”页面】
--------------------------------------------------

保持现有布局。

核心视觉重点为：

“未来充电窗口”。

13:00 是当前推荐时间。

强化：

推荐充电时间
13:00

预计 2 个空闲充电位

可以使用一个非常简单的时间轴。

时间轴：

12:00 ───── 13:00 ───── 14:00 ───── 15:00

13:00 使用青色 / 蓝紫色突出。

推荐时间节点可以使用：
圆点 + 光晕。

不要做复杂动画。

--------------------------------------------------
【二十四、动画】
--------------------------------------------------

动画只允许以下类型：

1. KPI 数字轻微滚动
2. 图表加载渐入
3. 站点节点轻微呼吸
4. 当前站点轻微光晕
5. 网络连接线非常缓慢的单个光点移动
6. Hover 时卡片轻微上浮
7. Tab 切换平滑过渡

动画时间：

200ms ~ 600ms 为普通 UI 动画。

站点呼吸：
2~4 秒。

网络流动：
4~8 秒。

禁止：
- 快速闪烁
- 大量粒子
- 全屏扫描
- 强烈霓虹
- 高频动画
- 自动旋转
- 复杂 3D 动画

--------------------------------------------------
【二十五、字体】
--------------------------------------------------

整体使用现代无衬线字体。

优先：

Inter
或
系统中文无衬线字体

中文可使用：
Microsoft YaHei / PingFang SC / Noto Sans SC

数字使用清晰现代字体。

数据数字需要明显大于普通文字。

建立三级信息层级：

一级：
核心数据数字

二级：
模块标题

三级：
辅助说明

--------------------------------------------------
【二十六、响应式】
--------------------------------------------------

这是一个大屏项目。

优先保证：
1920 × 1080

同时保证：
1366 × 768
1600 × 900

不允许因为 UI 改造导致：
- 卡片重叠
- 图表溢出
- 文字截断
- KPI 错位
- 地图区域变形

如果当前项目已经使用固定大屏缩放方案，则保留现有方案，不要重新设计响应式系统。

--------------------------------------------------
【二十七、代码要求】
--------------------------------------------------

必须优先复用现有组件。

如果项目已有：

Dashboard
Card
Chart
Station
KPI
Header

等组件，优先修改其样式，不要无意义重写业务代码。

ECharts option 可以重构视觉配置，但不要修改数据来源。

所有颜色尽量统一定义为 CSS Variables，例如：

--bg-page
--bg-card
--text-primary
--text-secondary
--color-primary
--color-blue
--color-purple
--color-warning
--color-danger
--border-light

避免在几十个组件中重复写颜色。

--------------------------------------------------
【二十八、禁止事项】
--------------------------------------------------

严禁：

1. 修改 Flask 后端
2. 修改 API
3. 修改数据库
4. 修改业务逻辑
5. 删除已有功能
6. 改变已有页面布局
7. 改变模块数量
8. 改变数据含义
9. 引入真实地图
10. 引入 Three.js
11. 引入 Cesium
12. 引入 Mapbox
13. 引入高德 / 百度地图
14. 制作真实 3D 城市
15. 制作复杂粒子系统
16. 使用赛博朋克视觉
17. 大量霓虹灯效果
18. 大面积纯黑背景
19. 使用高饱和彩虹渐变
20. 重新设计成普通后台管理系统

--------------------------------------------------
【二十九、最终视觉目标】
--------------------------------------------------

最终效果应该满足：

“第一眼是一个高级、明亮的新能源城市运营大屏。”

而不是：

“第一眼是一个传统蓝色科技大屏。”

重点视觉关系：

浅色背景
↓
白色数据卡片
↓
深蓝文字
↓
新能源青色作为核心视觉
↓
蓝色 / 紫色作为辅助
↓
橙色表示预警
↓
中央站点空间拓扑作为视觉核心
↓
少量动态体现实时性

整体应该：
简洁、明亮、专业、现代、未来感。

不要过度设计。

--------------------------------------------------
【三十、执行流程】
--------------------------------------------------

在开始修改代码之前：

1. 先扫描现有 Vue 项目结构。
2. 找到两个大屏页面对应的 Vue 文件。
3. 找到所有相关 CSS / SCSS / Less 文件。
4. 找到 ECharts 配置。
5. 找到站点空间分布的实现。
6. 确认现有 API 数据字段。
7. 确认哪些组件可以复用。

然后：

第一阶段：
只重构视觉 Design System。

第二阶段：
统一所有卡片。

第三阶段：
统一 KPI。

第四阶段：
统一 ECharts。

第五阶段：
重构中央“城市充电网络空间拓扑”。

第六阶段：
优化第二张“充电时空”页面。

第七阶段：
增加少量动画。

每完成一个阶段都要确保原有功能正常。

最终不要给我一个静态 HTML Demo。

必须直接修改现有 Vue 项目，使现有页面可以正常运行。

完成后检查：

- npm / pnpm / yarn 项目是否正常启动
- Vue 是否存在编译错误
- ECharts 是否存在运行错误
- 所有 API 是否仍然正常
- 页面布局是否保持原样
- 两个页面是否视觉统一
- 1366×768 和 1920×1080 是否正常